# The-Challenge


# Code die is geschreven: HTML, CSS, JavaScript en PHP
# -------------------------------------------------------------------------------------------------------------------------------------------#

# Alle documentatie staat in de File's gedocumenteerd per Blok tekst dat samen hangt 
# de documentatie bevat Front-end en Back-end.
# front-end is heel algemeen gedocumenteerd omdat dit makkelijk is op te zoeken en Simpeler te begrijpen is.
# back-end is een stuk complexer en daarom heb ik het zo makkelijk mogelijk uit te gelegd en bijna bij iedere code regel documentatie gezet. 
# 26/11/2025 10:36
# (Don)
# -------------------------------------------------------------------------------------------------------------------------------------------#

# zelf de Website hosten kan Via Local host (Uitleg)
# -------------------------------------------------------------------------------------------------------------------------------------------#
# Xampp / Mampp (voor IOS) word gedownload en de github files worden in de Directory HTDOCS gezet.
# Apache en MySql worden opgestart en MySql word Admin geselecteerd binnen de applicatie.
# Database word aangemaakt (Zie dbconnect.php) en maak de zelfde $db aan in je PhpMyAdmin.
# Maak de Tabel "client" aan en vul hem met de kolommen (Id, username, firstname, lastname, email, password en isadmin)

# id(int(4), Primery key, Ai/Auto increment)
# username(Varchar(256),)
# firstname(Varchar(256),)
# lastname(Varchar(256),)
# email(Varchar(256),)
# password(Varchar(256),)
# isadmin(tinyint(1), standaard waarde: 0) 
# (Don)
# -------------------------------------------------------------------------------------------------------------------------------------------#